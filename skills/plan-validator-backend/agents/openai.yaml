interface:
  display_name: "Backend Plan Validator"
  short_description: "Validate backend and API plans before they are saved."
  default_prompt: "Use $plan-validator-backend before saving or finalizing a backend or API plan to check original intent, unasked features, fallback-rule violations, plan format, API contracts, implementation-plan details, persistence coverage, rule compliance, and unit-test details."
