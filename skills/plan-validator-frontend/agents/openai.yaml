interface:
  display_name: "Plan Validator"
  short_description: "Validate plans before they are saved."
  default_prompt: "Use $plan-validator before saving or finalizing a plan to check original intent, unasked features, fallback-rule violations, plan format, rule compliance, and unit-test details."
