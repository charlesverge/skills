interface:
  display_name: "Business Rules Validator"
  short_description: "Validate business rules and rule-plan structure."
  default_prompt: "Use $business-validator-rules to validate and organize these business rules before implementation planning."
