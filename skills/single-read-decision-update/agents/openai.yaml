interface:
  display_name: "Single-Read Decision Update"
  short_description: "One-read one-write decision updates"
  default_prompt: "Use $single-read-decision-update to read entity state once, compute outcomes once, perform one write, return a DecisionResult with all caller-needed outcomes, and avoid same-entity re-reads in the same flow."
